import { Transform, Where, Group, GroupBy, GroupTransform, Having} from "./main";
import { describe, test, expect} from "vitest";

// Просто проверка на существование типов Transform, Where, Sort, Group, GroupBy, GroupTransform, Having
describe('Transform', () => {
    test('тип существует', () => {
        expect(true).toBe(true);
    });
});

describe('Where', () => {
    test('тип существует', () => {
        expect(true).toBe(true);
    });
});

describe('Sort', () => {
    test('тип существует', () => {
        expect(true).toBe(true);
    });
});

describe('Group', () => {
    test('тип существует', () => {
        expect(true).toBe(true);
    });
});

describe('GroupBy', () => {
    test('тип существует', () => {
        expect(true).toBe(true);
    });
});

describe('GroupTransform', () => {
    test('тип существует', () => {
        expect(true).toBe(true);
    });
});

describe('Having', () => {
    test('тип существует', () => {
        expect(true).toBe(true);
    });
});


