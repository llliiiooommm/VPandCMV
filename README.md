# Lab 7 — Книжные карточки на React

## Что за зверь

React-приложение, которое показывает карточки книг. В каждой карточке:
- Обложка (приходит как BLOB, чисто по заданию)
- Название книги (жирный, крупный шрифт)
- Авторы (поменьше, серенькие)

Карточки располагаются горизонтально и переносятся на следующую строку, если не влезают. Никаких таблиц — только flexbox.

## Откуда данные

Изначально в задании был `fakeapi.extendsclass.com`, но он отдавал 403 (доступ запрещён). Поэтому я пошёл другим путём:

- **Open Library API** — отдаёт список книг и ID обложек
- **Open Library Cover API** — тянет обложки по ID в виде BLOB
- **Google Books API** — запасной вариант, если у книги нет обложки в Open Library

Обложка грузится как BLOB → превращается в URL через `URL.createObjectURL()` → показывается в `<img>`.

## Как запустить

```bash
npm install
npm run dev
```
Откроется `http://localhost:NNNN`

## Что умеет

- 🔍 Поиск книг по названию (мгновенный, без кнопок)
- 📊 Счётчик найденных книг
- 🎨 Плавные ховеры (карточка подлетает)
- 📱 Адаптивная сетка

## Структура проекта (важное)

```
src/
├── api/               # запросы к API
│   ├── openLibraryApi.ts
│   ├── openLibraryCoverApi.ts
│   └── googleBooksApi.ts
├── components/
│   └── BookCard/
│       ├── BookCard.tsx
│       └── BookCard.css
├── hooks/
│   └── useBooks.ts    # загрузка книг + обложек
├── types/
│   └── book.ts        # TypeScript интерфейсы
├── App.tsx
├── App.css
└── main.tsx
```
## Что было сложным

- CORS и битые API — пришлось искать рабочий источник данных
- BLOB → URL → очистка памяти через `revokeObjectURL`
- Асинхронная загрузка обложек для 12 книг одновременно

## ⚠️ Проблемы и решения

### Проблема 1: Оригинальный API не работал
API из задания (`fakeapi.extendsclass.com`) возвращал ошибку 403.  
**Решение:** Использовал Open Library API — он стабильный и без CORS.

### Проблема 2: Долгая загрузка и Network Error
При попытке загрузить обложки через Google Books API происходил таймаут (15-20 сек) и ошибка.  
**Решение:** Убрал Google Books, оставил только Open Library Cover API. Обложки, которых нет, заменяются на иконку 📚.

### Проблема 3: Ошибка в одной обложке ломала все книги
Если одна обложка не загружалась — падал весь Promise.all.  
**Решение:** Обернул загрузку каждой обложки в try/catch. Теперь одна неудача не убивает остальные.

## 📺 Мои эмоции во время этой работы:

<p align="center">
	<img src="./assets/alya.gif" width="45%" alt="demo" />
</p>

---

_Выполнено в рамках учебного курса по визуальному программированию, 2026._
